[
  {
    "title": "Claude Code MAX のレート制限をステータスラインに常時表示",
    "tags": [
      "claude-code",
      "statusline",
      "rate-limit"
    ],
    "content": "Claude Code MAXでは5時間枠・7日枠のレート制限がある。ステータスラインに常時表示すれば、いちいち確認しに行く手間がなくなる。\n\n### 手順\n\n1. スクリプトを作成:\n\n```bash\nmkdir -p ~/.claude/scripts\ncat > ~/.claude/scripts/statusline.sh << 'EOF'\n#!/bin/bash\ninput=$(cat)\nMODEL=$(echo \"$input\" | jq -r '.model.display_name')\nCTX=$(echo \"$input\" | jq -r '.context_window.used_percentage // empty')\nFIVE_H=$(echo \"$input\" | jq -r '.rate_limits.five_hour.used_percentage // empty')\nWEEK=$(echo \"$input\" | jq -r '.rate_limits.seven_day.used_percentage // empty')\n\nparts=\"\"\n[ -n \"$FIVE_H\" ] && parts=\"5h:$(printf '%.0f' \"$FIVE_H\")%\"\n[ -n \"$WEEK\" ] && parts=\"${parts:+$parts }7d:$(printf '%.0f' \"$WEEK\")%\"\n[ -n \"$CTX\" ] && parts=\"${parts:+$parts }ctx:${CTX}%\"\n\necho \"[$MODEL] $parts\"\nEOF\nchmod +x ~/.claude/scripts/statusline.sh\n```\n\n2. `~/.claude/settings.json` に追加:\n\n```json\n{\n  \"statusLine\": {\n    \"type\": \"command\",\n    \"command\": \"~/.claude/scripts/statusline.sh\"\n  }\n}\n```\n\n### 表示例\n\n```\n[Opus] 5h:23% 7d:41% ctx:8%\n```\n\n- `5h` — 5時間枠のレート制限使用率\n- `7d` — 7日枠のレート制限使用率\n- `ctx` — コンテキストウィンドウ使用率（会話の長さ）\n\n### 備考\n\n- APIレスポンスのたびに自動更新（トークン消費なし）\n- `jq` が必要（`brew install jq`）\n- レート制限の値は最初のAPIレスポンス後に表示される"
  },
  {
    "title": "python-pptx でテンプレートのレイアウトを活用してパワポを生成する",
    "tags": [
      "pptx",
      "python",
      "テンプレート"
    ],
    "content": "既存のパワポからテンプレートを抽出し、スライドマスター（フォント・配色・背景）を活かして安定した品質のパワポを自動生成する方法。\n\n### 全体フロー\n\n1. テンプレート抽出（既存pptxからクリーン版を作成）\n2. config.jsonにレイアウト定義を保存\n3. 生成時はテンプレートのレイアウトを使い、プレースホルダー+図形描画で作成\n\n### 前提\n\n- `pip install python-pptx` でインストール済み\n- ベースとなるpptxファイル（社内テンプレートや既存の資料）を用意する\n- レイアウト定義を管理するconfig.json（テンプレートパス・フォント設定等）を用意する\n\n### テンプレート抽出手順\n\n```python\n# 1. レイアウト構造を把握するスクリプトを用意して実行\n# （各レイアウトのインデックス・プレースホルダー情報を出力する）\n\n# 2. 各レイアウトでサンプルスライドを作成 → 元スライドを全削除 → 保存\nprs = Presentation(\"元のピッチ資料.pptx\")\n\n# レイアウトごとにサンプルスライド追加\nslide = prs.slides.add_slide(prs.slide_layouts[0])  # 表紙\nslide = prs.slides.add_slide(prs.slide_layouts[4])  # セクション区切り\n# ... 必要なレイアウトを全部追加\n\n# 元のスライドを削除\nns_r = '{http://schemas.openxmlformats.org/officeDocument/2006/relationships}'\nfor i in range(ORIGINAL_SLIDE_COUNT):\n    sldId = prs.slides._sldIdLst[0]\n    rId = sldId.get(f'{ns_r}id')\n    if rId:\n        prs.part.drop_rel(rId)\n    prs.slides._sldIdLst.remove(sldId)\n\nprs.save(\"clean-template.pptx\")\n```\n\n### 生成時のコード構造\n\n```python\nimport json\nfrom pptx import Presentation\n\n# config.jsonからテンプレートパス・レイアウト定義を読む\n# config.json にはテンプレートのパス、デフォルトフォント、レイアウト定義等を記載\nwith open(\"config.json\") as f:\n    CONFIG = json.load(f)\n\nprs = Presentation(CONFIG[\"template_path\"])\n\n# レイアウト選択 → プレースホルダーにテキスト → コンテンツエリアに図形描画\nslide = prs.slides.add_slide(prs.slide_layouts[2])  # コンテンツA（メイン）\nslide.placeholders[1].text = \"章タイトル\"\n\n# 図形を描画（コンテンツエリア内に配置）\n# ...\n\n# テンプレートのサンプルスライド削除 → 保存\n```\n\n### レイアウト一覧の把握\n\nテンプレートのレイアウトはpptxごとに異なる。事前に以下のスクリプトで一覧を出力し、config.jsonに記録しておくとよい:\n\n```python\nfrom pptx import Presentation\nprs = Presentation(\"your-template.pptx\")\nfor i, layout in enumerate(prs.slide_layouts):\n    print(f\"Layout {i}: {layout.name}\")\n    for ph in layout.placeholders:\n        print(f\"  idx={ph.placeholder_format.idx}: {ph.placeholder_format.type}, {ph.width}x{ph.height}\")\n```\n\n典型的な構成例:\n\n- Layout 0: 表紙（idx0=タイトル, idx1=サブタイトル）\n- Layout 1〜5: コンテンツ系（idx1=章タグ等）\n- セクション区切り（idx0=番号, idx1=章タイトル）\n- 白紙キャンバス\n\n### フォント注意\n\n- macOSでは **Hiragino Sans** を使う（Meiryoは入っていない）\n- テンプレートのテーマフォントと別に、図形描画時にフォントを明示指定すること\n- config.jsonの `default_font` で一元管理\n\n### config.json の例\n\n```json\n{\n  \"template_path\": \"./your-template.pptx\",\n  \"default_font\": \"Hiragino Sans\",\n  \"layouts\": {\n    \"cover\": 0,\n    \"content_main\": 2,\n    \"section\": 4,\n    \"blank\": 7\n  }\n}\n```"
  },
  {
    "title": "既存pptxのフォーマットを維持してコンテンツだけ差し替える（2段階方式）",
    "tags": [
      "pptx",
      "python",
      "テンプレート",
      "フォーマット維持"
    ],
    "content": "クライアントや他社のブランドpptxをフォーマットとして使い、中身だけ差し替えたい場合の手順。deexテンプレート方式とは別に、任意のpptxをベースにできる。\n\n### 全体フロー\n\n1. **Step 1: フォーマットスライド生成** — 元pptxからスライドを複製して空の器を作る\n2. **Step 2: コンテンツ注入** — 器の上にカード・表・図解を図形として配置\n3. **Step 3: 配布** — Googleドライブにコピー or pptxを直接共有\n\n### Step 1: スライド複製（フォーマット確保）\n\n```python\nfrom pptx import Presentation\nimport copy\n\nprs = Presentation(\"元のブランドpptx.pptx\")\n\ndef dup_slide(prs, index):\n    \"\"\"既存スライドを丸ごと複製して末尾に追加\"\"\"\n    source = prs.slides[index]\n    new_slide = prs.slides.add_slide(source.slide_layout)\n    spTree = new_slide.shapes._spTree\n    # レイアウトから来たデフォルトシェイプを消す\n    for child in list(spTree):\n        if child.tag.endswith('}sp') or child.tag.endswith('}pic') or \\\n           child.tag.endswith('}grpSp') or child.tag.endswith('}cxnSp'):\n            spTree.remove(child)\n    # ソーススライドのシェイプを丸コピー\n    for elem in source.shapes._spTree:\n        if elem.tag.endswith('}sp') or elem.tag.endswith('}pic') or \\\n           elem.tag.endswith('}grpSp') or elem.tag.endswith('}cxnSp'):\n            spTree.append(copy.deepcopy(elem))\n    return new_slide\n\n# 使いたいスライドを選んで複製（0始まり）\n# 例: 表紙=0, セクション区切り=1, コンテンツ=25 ...\nfor src_idx in [0, 1, 25, 25, 25]:\n    dup_slide(prs, src_idx)\n\n# 元スライドを全削除\nns_r = '{http://schemas.openxmlformats.org/officeDocument/2006/relationships}'\noriginal_count = 38  # 元のスライド枚数\nfor i in range(original_count):\n    sldId = prs.slides._sldIdLst[0]\n    rId = sldId.get(f'{ns_r}id')\n    if rId:\n        prs.part.drop_rel(rId)\n    prs.slides._sldIdLst.remove(sldId)\n\nprs.save(\"format-base.pptx\")\n```\n\n### Step 2: コンテンツ注入\n\n```python\nfrom pptx import Presentation\nfrom pptx.util import Pt, Cm\nfrom pptx.dml.color import RGBColor\nfrom pptx.enum.text import PP_ALIGN\nfrom pptx.enum.shapes import MSO_SHAPE\n\nprs = Presentation(\"format-base.pptx\")\n\n# ── ヘルパー関数 ──\n\ndef clear_all_shapes(slide):\n    \"\"\"スライド内の全テキストをクリア（フォーマット図形は残る）\"\"\"\n    for shape in slide.shapes:\n        if shape.has_text_frame:\n            for p in shape.text_frame.paragraphs:\n                for run in p.runs:\n                    run.text = \"\"\n    for ph in slide.placeholders:\n        if ph.has_text_frame:\n            for p in ph.text_frame.paragraphs:\n                for run in p.runs:\n                    run.text = \"\"\n\ndef set_title(slide, text):\n    \"\"\"スライドのタイトルを設定（プレースホルダー or 最上部シェイプ）\"\"\"\n    for ph in slide.placeholders:\n        if ph.placeholder_format.idx == 0:\n            if ph.text_frame.paragraphs and ph.text_frame.paragraphs[0].runs:\n                ph.text_frame.paragraphs[0].runs[0].text = text\n            else:\n                ph.text = text\n            return\n    # プレースホルダーがなければ最上部のテキストシェイプ\n    top_shape = None\n    for shape in slide.shapes:\n        if shape.has_text_frame:\n            if top_shape is None or shape.top < top_shape.top:\n                top_shape = shape\n    if top_shape and top_shape.text_frame.paragraphs:\n        if top_shape.text_frame.paragraphs[0].runs:\n            top_shape.text_frame.paragraphs[0].runs[0].text = text\n            for run in top_shape.text_frame.paragraphs[0].runs[1:]:\n                run.text = \"\"\n\ndef tb(slide, left, top, width, height, text, size=10, bold=False,\n       color=RGBColor(0x33,0x33,0x33), align=PP_ALIGN.LEFT, font=\"Hiragino Sans\"):\n    \"\"\"テキストボックス追加\"\"\"\n    box = slide.shapes.add_textbox(Cm(left), Cm(top), Cm(width), Cm(height))\n    box.text_frame.word_wrap = True\n    p = box.text_frame.paragraphs[0]\n    p.alignment = align\n    run = p.add_run()\n    run.text = text\n    run.font.size = Pt(size)\n    run.font.bold = bold\n    run.font.color.rgb = color\n    run.font.name = font\n    return box\n\ndef rect(slide, left, top, width, height, fill, border=None):\n    \"\"\"矩形追加\"\"\"\n    shape = slide.shapes.add_shape(\n        MSO_SHAPE.ROUNDED_RECTANGLE, Cm(left), Cm(top), Cm(width), Cm(height))\n    shape.fill.solid()\n    shape.fill.fore_color.rgb = fill\n    if border:\n        shape.line.color.rgb = border\n        shape.line.width = Pt(1)\n    else:\n        shape.line.fill.background()\n    return shape\n\n# ── 各スライドの処理 ──\n# パターン: clear_all_shapes → set_title → tb/rect で図形配置\n\nslide = prs.slides[0]\nclear_all_shapes(slide)\nset_title(slide, \"提案タイトル\")\ntb(slide, 1.5, 4.0, 22.0, 1.0, \"本文テキスト\", size=11)\n\n# ... 各スライドに対して同様の処理 ...\n\nprs.save(\"output.pptx\")\n```\n\n### Step 3: 共有\n\n生成した output.pptx を共有先に配置する。方法はいくつかある:\n\n```bash\n# Google Drive MCPで直接アップロード\n# mcp__google-drive__uploadFile でフォルダIDを指定\n\n# macOSでGoogle Driveのローカル同期フォルダにコピーする場合\ncp output.pptx ~/Library/CloudStorage/GoogleDrive-xxx/マイドライブ/共有フォルダ/\n\n# またはSlack等で直接共有\n```\n\n### カラーパレット抽出（元pptxから）\n\n新しいブランドpptxを使う際、まず配色を抽出して準拠する:\n\n```python\nfrom pptx import Presentation\nprs = Presentation(\"brand.pptx\")\ncolors = set()\nfor slide in prs.slides:\n    for shape in slide.shapes:\n        if shape.has_text_frame:\n            for p in shape.text_frame.paragraphs:\n                for run in p.runs:\n                    try:\n                        if run.font.color and run.font.color.rgb:\n                            colors.add((\"font\", str(run.font.color.rgb)))\n                    except: pass\n        try:\n            if shape.fill.type is not None:\n                colors.add((\"fill\", str(shape.fill.fore_color.rgb)))\n        except: pass\nfor ctype, rgb in sorted(colors):\n    print(f\"#{rgb} ({ctype})\")\n```\n\n### 注意事項\n\n- `clear_all_shapes` はテキストだけ消す。図形の背景・罫線・ヘッダーバーなどは残る\n- `inject-content.py` は冪等ではない（2回実行すると図形が重複する）。再実行時はStep 1からやり直す\n- スライドの複製時、プレースホルダーのテキストは消えがちだが、on-slideシェイプはコピーされる\n- ブランドのデザイン要素がスライドマスターではなくon-slideシェイプにある場合が多い（特にGoogle Slides由来のpptx）\n\n### 実績\n\n- hacomono PRM再稟議資料（2026-04）: `_workspace/hacomono-re-proposal/inject-content.py`"
  },
  {
    "title": "Google Drive MCP で Claude Code からスプレッドシート・スライドを直接読み書きする",
    "tags": [
      "google-drive",
      "mcp",
      "スプレッドシート",
      "スライド",
      "評価シート"
    ],
    "content": "Claude Code から Google Drive のファイル閲覧・スプレッドシートの読み書き・スライド（PowerPoint/Google Slides）操作・フォルダ操作ができる。評価シート記入やスライド編集など、ブラウザを開かずに完結できる。\n\n### 前提\n\n- Claude Code MAX（MCP対応）\n- GCPプロジェクトでOAuth同意画面を設定済み\n- テストユーザーに自分のGoogleアカウントを追加済み\n\n### セットアップ手順\n\n#### 1. GCPプロジェクト作成・OAuth設定\n\n1. [Google Cloud Console](https://console.cloud.google.com/) でプロジェクトを作成\n2. 「APIとサービス」→「OAuth同意画面」を設定（外部 / テストモード）\n3. テストユーザーに自分のメールアドレスを追加\n4. 「認証情報」→「OAuthクライアントID」を作成（デスクトップアプリ）\n5. JSONをダウンロード\n\n#### 2. credentials配置\n\n```bash\nmkdir -p ~/.config/google-drive-mcp\nmv ~/Downloads/client_secret_*.json ~/.config/google-drive-mcp/gcp-oauth.keys.json\n```\n\n#### 3. Claude Code MCP設定\n\n`~/.claude/settings.json` に追加:\n\n```json\n{\n  \"mcpServers\": {\n    \"google-drive\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@anthropic-ai/google-drive-mcp\"],\n      \"env\": {\n        \"GOOGLE_CREDENTIALS_PATH\": \"/Users/yourname/.config/google-drive-mcp/gcp-oauth.keys.json\",\n        \"GOOGLE_TOKEN_PATH\": \"/Users/yourname/.config/google-drive-mcp/tokens.json\"\n      }\n    }\n  }\n}\n```\n\n#### 4. 初回認証\n\nClaude Codeを起動（または再起動）すると、初回のみブラウザが開いてGoogleログイン画面が出る。許可すると `tokens.json` が自動生成され、以降はブラウザ不要。\n\n### 主な操作\n\n#### フォルダの中身を見る\n\n```\nmcp__google-drive__listFolder(folderId: \"フォルダID\")\n```\n\n- フォルダIDはGoogle DriveのURL末尾: `drive/folders/XXXXX` の `XXXXX` 部分\n\n#### スプレッドシートのシート一覧を確認\n\n```\nmcp__google-drive__listSheets(spreadsheetId: \"スプレッドシートID\")\n```\n\n- スプレッドシートIDはURLの `/d/XXXXX/` の `XXXXX` 部分\n\n#### セルの読み取り\n\n```\nmcp__google-drive__getGoogleSheetContent(\n  spreadsheetId: \"ID\",\n  range: \"シート名!A1:Z50\"\n)\n```\n\n- シート名に日本語・記号が含まれていてもそのまま指定OK\n- `Sheet1` ではなく実際のシート名（例: `202510-202603`）を使うこと\n\n#### セルへの書き込み\n\n```\nmcp__google-drive__updateGoogleSheet(\n  spreadsheetId: \"ID\",\n  range: \"シート名!L19\",\n  data: [[\"書き込みたいテキスト\"]]\n)\n```\n\n- `data` は2D配列。複数セルに一括書き込みも可能: `[[\"A1の値\", \"B1の値\"], [\"A2の値\", \"B2の値\"]]`\n- デフォルトはRAWモード（数式は文字列として保存）\n\n#### ファイル検索\n\n```\nmcp__google-drive__search(query: \"評価シート\")\n```\n\n### 活用例\n\n| ユースケース | 操作 |\n|-------------|------|\n| 評価シートの上長コメント記入 | listFolder → listSheets → getContent → updateSheet |\n| 事業計画Excelの数値確認 | getContent で該当範囲を読み取り |\n| 資料のフォルダ整理 | listFolder → moveItem |\n| 会議前にアジェンダ確認 | getGoogleDocContent でドキュメント読み取り |\n\n### ID の見つけ方\n\n| 対象 | URL例 | IDの位置 |\n|------|-------|---------|\n| フォルダ | `drive.google.com/drive/folders/XXXXX` | `XXXXX` |\n| スプレッドシート | `docs.google.com/spreadsheets/d/XXXXX/edit` | `XXXXX` |\n| ドキュメント | `docs.google.com/document/d/XXXXX/edit` | `XXXXX` |\n| スライド | `docs.google.com/presentation/d/XXXXX/edit` | `XXXXX` |\n\n### 注意事項\n\n- OAuth同意画面が「テストモード」の場合、テストユーザーに登録したアカウントのみアクセス可能\n- トークンは定期的に失効する。認証エラーが出たら `tokens.json` を削除してClaude Code再起動\n- 大量のセル書き込みは1リクエストでまとめた方が効率的（range指定で複数セル一括）\n- `Sheet1` というデフォルト名ではなく、実際のシート名を `listSheets` で確認してから操作すること"
  },
  {
    "title": "GitHub Pages で社内ナレッジを外部公開する",
    "tags": [
      "github-pages",
      "公開",
      "静的サイト"
    ],
    "content": "ローカルやVPN内でしか見られないダッシュボードの情報を、社外メンバーにも共有したいときに使える方法。GitHub Pages なら無料で、git push するだけで更新される。\n\n### 考え方\n\n- SSoT（原本）はローカルの `.company/` 内に置いたまま\n- 変換スクリプトで公開用データ（JSON等）を生成\n- 専用の公開リポジトリに push → GitHub Pages で自動デプロイ\n- 機密情報が入ったリポジトリとは完全に分離する\n\n### 手順\n\n#### 1. 公開用リポジトリを作成\n\n```bash\nmkdir ~/my-public-site\ncd ~/my-public-site\ngit init\n\n# docs/ に静的ファイルを配置（GitHub Pages のソースにする）\nmkdir docs\n# → docs/index.html, docs/style.css, docs/app.js 等を作成\n\ngit add -A\ngit commit -m \"Initial commit\"\n\n# GitHub にパブリックリポジトリを作成 & push\ngh repo create ユーザー名/リポジトリ名 --public --source=. --push --description \"説明\"\n```\n\n#### 2. GitHub Pages を有効化\n\n```bash\ngh api repos/ユーザー名/リポジトリ名/pages \\\n  -X POST \\\n  -f \"build_type=legacy\" \\\n  -f \"source[branch]=main\" \\\n  -f \"source[path]=/docs\"\n```\n\n数十秒後に `https://ユーザー名.github.io/リポジトリ名/` で公開される。\n\n#### 3. SSoT → 公開データの変換スクリプトを作る\n\n```python\n#!/usr/bin/env python3\n# build.py — 例: Markdownファイル → JSON変換\nimport json, re\nfrom pathlib import Path\n\nSRC = Path.home() / \".company\" / \"operations\" / \"元ファイル.md\"\nOUT = Path(__file__).parent / \"docs\" / \"data.json\"\n\n# Markdownをパースしてデータを抽出\ntext = SRC.read_text(encoding=\"utf-8\")\nitems = []  # パース結果をリストに格納\n# ... パース処理 ...\n\nOUT.parent.mkdir(exist_ok=True)\nOUT.write_text(json.dumps(items, ensure_ascii=False, indent=2))\n```\n\n#### 4. 同期スクリプトを作る\n\n```bash\n#!/bin/bash\n# sync.sh — ビルド → 差分があれば push\nset -euo pipefail\ncd \"$(dirname \"$0\")\"\n\npython3 build.py\n\ngit add docs/\nif git diff --cached --quiet; then\n  echo \"No changes\"\n  exit 0\nfi\n\ngit commit -m \"update $(date +%Y-%m-%d)\"\ngit push origin main\necho \"✅ Synced\"\n```\n\n#### 5. ファイル変更時に自動同期（macOS launchd）\n\n```xml\n<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\"\n  \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\">\n<plist version=\"1.0\">\n<dict>\n    <key>Label</key>\n    <string>com.example.my-sync</string>\n    <key>ProgramArguments</key>\n    <array>\n        <string>/bin/bash</string>\n        <string>-c</string>\n        <string>cd ~/my-public-site &amp;&amp; ./sync.sh</string>\n    </array>\n    <key>WatchPaths</key>\n    <array>\n        <string>/Users/ユーザー名/.company/operations/元ファイル.md</string>\n    </array>\n</dict>\n</plist>\n```\n\n```bash\ncp com.example.my-sync.plist ~/Library/LaunchAgents/\nlaunchctl load ~/Library/LaunchAgents/com.example.my-sync.plist\n```\n\n`WatchPaths` で指定したファイルが変更されるたびに sync.sh が自動実行される。\n\n### 静的サイトの構成パターン\n\n| ファイル | 役割 |\n|---------|------|\n| `docs/index.html` | ページ本体（HTMLシェル） |\n| `docs/style.css` | スタイル |\n| `docs/app.js` | データ読み込み・描画・フィルタリング |\n| `docs/data.json` | build.py で生成したデータ（git管理） |\n| `build.py` | SSoT → data.json 変換 |\n| `sync.sh` | ビルド → git push |\n\n### 注意事項\n\n- **パブリックリポジトリのみ** GitHub Pages 無料（プライベートは Pro/Teams 必要）\n- 公開リポジトリには機密情報を絶対に入れない。変換スクリプトで必要な部分だけ抽出する\n- 組織アカウントの開発用リポジトリとは別に、個人アカウントで公開用リポジトリを作るのが安全\n- CDN（jsDelivr等）からライブラリを読み込めば、ビルドツール不要で静的HTMLだけで済む\n\n### 実績\n\n- deex Tips 公開サイト: `kento989t-spec/deex-tips` → https://kento989t-spec.github.io/deex-tips/\n  - SSoT: `.company/operations/tips.md`\n  - 同期: `~/deex-tips/sync.sh` + launchd `WatchPaths`"
  },
  {
    "title": "Claude Code MAX で事業運営を自律化する（全体アーキテクチャ）",
    "tags": [
      "自律運営",
      "claude-code",
      "アーキテクチャ"
    ],
    "content": "Claude Code MAX をスケジューラ（macOS launchd）で定期起動し、メール取得・TODO管理・調査・レポート作成を自動実行する仕組みの全体像。人間は「承認と判断」だけに集中できる。\n\n### コンセプト: 思考と承認の分離\n\n- **Claude（自動）**: 情報収集、分析、下書き作成、整理、TODO消化\n- **人間（承認）**: 外部連絡、記事公開、方針決定、レビュー\n\nこの分離をTODOのタグ（`#auto` / `#human` / `#queue`）で実現する。\n\n### 全体フロー\n\n```\nメール / Slack / 手動入力\n        ↓\n  secretary/todos/YYYY-MM-DD.md にTODO化\n        ↓\n  classify-todo.sh でタグ自動付与\n  ┌──────┬──────┬──────┐\n  ↓      ↓      ↓      ↓\n#auto  #human #queue  タグなし→#human\n  ↓      ↓      ↓\n  ↓    朝まとめ  承認待ち\n  ↓    通知     キュー\n  ↓   (08:00)\n  ↓\nactivity-report.sh が6時間ごとにピックアップ＆実行\n        ↓\n  結果をファイルに記録 + dashboard.md 更新\n        ↓\n  Telegram で実行報告\n        ↓\n  22:00 に TODO 日次見直し（重複統合・期限超過チェック）\n```\n\n### 構成要素\n\n| レイヤー | 役割 | 実体 |\n|---------|------|------|\n| SSoT（事業の現在地） | KPI・ブロッカー・進捗の一元管理 | `operations/dashboard.md` |\n| TODO管理 | 日次タスクの収集・分類・消化 | `secretary/todos/YYYY-MM-DD.md` |\n| 受信トレイ | メール・Slackからの着信記録 | `secretary/inbox/YYYY-MM-DD.md` |\n| タグ分類 | TODO の自動振り分け | `operations/scripts/classify-todo.sh` |\n| 自動実行 | #auto タスクの定期消化 | `operations/scripts/activity-report.sh` |\n| 朝まとめ | #human タスクの朝通知 | `operations/scripts/morning-digest.sh` |\n| 日次見直し | 重複統合・プロジェクト昇格 | `operations/scripts/todo-daily-review.sh` |\n| メール取得 | IMAP → Claude トリアージ | `operations/scripts/check-email.sh` |\n| 通知 | 実行結果の即時通知 | `operations/scripts/notify-telegram.sh` |\n| スケジューラ | 各スクリプトの定期実行 | `operations/launchd/*.plist` |\n| タスクプロンプト | 調査・記事作成等の実行指示 | `operations/task-prompts/*.md` |\n| キュー | 承認待ちアイテムの管理 | `operations/queue/*.json` |\n\n### スケジュール一覧\n\n| 時刻 | ジョブ | 内容 |\n|------|--------|------|\n| 毎朝 07:30 | check-email | メール取得 → トリアージ → TODO化 |\n| 毎朝 08:00 | morning-digest | #human TODO を Telegram 通知 |\n| 6時間ごと | activity-report | #auto TODO 1〜2件を選んで実行 |\n| 火・木 09:00 | industry-research | PRM・パートナービジネス市場調査 |\n| 水 09:00 | competitor-watch | 競合動向の監視 |\n| 木 14:00 | regulatory-watch | IPO・規制変更の追跡 |\n| 金 10:00 | note-article | note.com 記事ドラフト作成 |\n| 金 17:00 | weekly-kpi | 週次KPI集計 |\n| 毎晩 22:00 | todo-daily-review | TODO の重複統合・期限超過チェック |\n\n### SSoT（Single Source of Truth）マップ\n\n| 情報 | 正のファイル |\n|------|------------|\n| 事業の現在地・KPI | `operations/dashboard.md` |\n| プロジェクト進捗 | `pm/projects/*.md` |\n| TODO | `secretary/todos/YYYY-MM-DD.md` |\n| 意思決定ログ | `secretary/notes/YYYY-MM-DD-decisions.md` |\n| 学び | `secretary/notes/YYYY-MM-DD-learnings.md` |\n| 業界調査 | `research/topics/*.md` |\n| 競合情報 | `research/topics/competitor-*.md` |\n| note記事戦略 | `marketing/content-plan/note-*.md` |\n\n### 前提環境\n\n- Claude Code MAX（`claude -p` でプロンプト実行、`--dangerously-skip-permissions` で自律実行）\n- macOS（launchd でスケジュール実行）\n- Mac Mini を常時起動マシンとして使用\n- Tailscale VPN で外部端末からもアクセス可能\n- Obsidian で `.company/` を開くと全ファイルがリンク付きで閲覧・検索可能"
  },
  {
    "title": "Slack・メールから TODO を自動作成し #auto / #human / #queue に振り分ける",
    "tags": [
      "自律運営",
      "todo",
      "slack",
      "メール",
      "分類"
    ],
    "content": "Slack やメールで来たタスクを `secretary/todos/` に集約し、自動実行できるもの・人間が対応すべきもの・承認待ちのものに自動分類する。\n\n### 3つのタグ\n\n| タグ | 意味 | 扱い |\n|------|------|------|\n| `#auto` | 自動実行OK（調査、資料作成、整理、まとめ等） | activity-report.sh が6時間ごとにピックアップして実行 |\n| `#human` | 人間アクション必要（確認、連絡、承認、レビュー等） | 朝まとめ通知で報告のみ。自動実行しない |\n| `#queue` | 承認待ち（公開、デプロイ、提出等） | キューに記録。オーナー承認後に実行 |\n\n### TODO のフォーマット\n\n```markdown\n- [ ] タスク内容 | 優先度: 高/通常/低 | 期限: YYYY-MM-DD | #auto\n- [ ] Slack由来のタスク | 優先度: 高 | 期限: 2026-04-15 | #human\n  - ref: https://deexcoltd.slack.com/archives/C0XXX/p1234567890\n- [x] 完了タスク | 完了: 2026-04-10\n```\n\n- 1日1ファイル: `secretary/todos/YYYY-MM-DD.md`\n- 同じ日付のファイルが既にあれば追記（新規作成しない）\n- Slack由来のTODOは `ref:` でpermalinkを行内に埋め込む（統合・繰越時にも消さない）\n\n### メールからの TODO 化\n\n`check-email.sh` が毎朝 07:30 に実行:\n\n1. **IMAP でメール取得**（`check-email.py`）\n   - お名前.com のメールサーバーから未読メールを取得\n   - 認証情報は macOS Keychain に保存（スクリプトにハードコードしない）\n2. **Claude でトリアージ**\n   - 🔴 要対応（返信・承認・アクション必要）→ `#human` TODO を作成\n   - 🟡 確認（目を通すべき）→ inbox に記録\n   - ⚪ スキップ（営業メール、メルマガ）→ 件数のみ記録\n3. **結果を記録**: `secretary/inbox/YYYY-MM-DD.md` に追記\n4. **Telegram 通知**: 完了 or エラーを通知\n\n```bash\n# check-email.sh の実行（手動）\ncd ~/.company && ./operations/scripts/check-email.sh\n\n# 過去3日分を取得\n./operations/scripts/check-email.sh --days 3\n```\n\n### Slack からの TODO 化\n\nSlack のメッセージからTODOを作る場合は、秘書（Claude Code）に依頼する:\n\n```\n「このSlackメッセージをTODOにして」+ permalinkを貼り付け\n```\n\n秘書が `secretary/todos/YYYY-MM-DD.md` に追記し、`classify-todo.sh` でタグを自動付与する。\n\n### 自動分類の仕組み（classify-todo.sh）\n\nパターンマッチで判別。優先順位: `#queue` > `#human` > `#auto`\n\n```bash\n# #human になるパターン\n確認|依頼|連絡|返信|承認|相談|電話|面談|レビュー|送付|報告|メール|\n送信|ミーティング|MTG|打ち合わせ|Slack.*返|校閲|対応|フォーム入力\n\n# #queue になるパターン\n公開|デプロイ|deploy|リリース|release|提出|submit|配信\n\n# #auto になるパターン\n調査|調べ|リサーチ|research|作成|ドラフト|下書き|整理|まとめ|\n分析|比較|リスト|一覧|更新|テンプレ|設計|ドキュメント|資料.*準備\n```\n\n**安全側に倒す**: Slackリンク付き → `#human`、判別不能 → `#human`\n\n```bash\n# 手動で全TODOを再分類\n./operations/scripts/classify-todo.sh\n\n# 特定ファイルだけ\n./operations/scripts/classify-todo.sh secretary/todos/2026-04-10.md\n```\n\n### 分類結果の例\n\n| TODO | 判定 | 理由 |\n|------|------|------|\n| 「PRM市場の最新動向を調査」 | `#auto` | 「調査」にマッチ |\n| 「牛丸さんにnote記事レビュー依頼」 | `#human` | 「依頼」にマッチ |\n| 「note第4弾を公開」 | `#queue` | 「公開」にマッチ |\n| 「[Slack:#general] 対応お願いします」 | `#human` | Slackリンク付き |"
  },
  {
    "title": "#auto タスクを Claude Code MAX に定期実行させる",
    "tags": [
      "自律運営",
      "自動実行",
      "activity-report",
      "launchd"
    ],
    "content": "`#auto` タグが付いた TODO を Claude Code MAX が6時間ごとに自動で消化する仕組み。調査・資料作成・整理系のタスクを人間の介入なしで完了できる。\n\n### 仕組み\n\n`activity-report.sh` が6時間ごとに launchd で起動し、以下を実行:\n\n1. **状態把握**: `dashboard.md` → プロジェクト → TODO → inbox を順に読む\n2. **タスク選定**: `#auto` の未完了TODOから1〜2件をピックアップ\n3. **実行**: Claude Code MAX がタスクを実行（調査ならWebSearch、資料作成ならファイル生成等）\n4. **記録**: 結果をファイルに書き込み、`dashboard.md` の実行ログを更新、TODOを完了に\n5. **通知**: Telegram で実行報告を送信\n\n### タスク選定の優先順位\n\n1. 期限が迫っている `#auto` TODO\n2. ブロッカー解消に貢献するタスク\n3. 定期調査（業界調査・競合監視・規制動向）\n4. コンテンツパイプライン前進（記事ネタ調査・ドラフト作成）\n5. `dashboard.md` の情報更新\n\n### タグ別の扱い\n\n| タグ | 扱い |\n|------|------|\n| `#auto` | 積極的にピックアップして実行 |\n| `#human` | 実行しない。「⚠️ 要確認」セクションに記載のみ |\n| `#queue` | 実行しない。承認待ちとして報告のみ |\n| タグなし | 内容を見て判断。外部連絡・承認系は実行しない |\n\n### 実行時に Claude に渡す権限\n\n```bash\nclaude -p \"$PROMPT\" \\\n    --allowedTools \"Read,Write,Edit,Glob,Grep,Bash,WebSearch,WebFetch,Agent\" \\\n    --dangerously-skip-permissions\n```\n\n- ファイル読み書き、Web検索、Bash実行を許可\n- `--dangerously-skip-permissions` で確認プロンプトなし（自律実行のため）\n- 外部通知（Slack送信、メール送信等）のツールは渡さない → 安全弁\n\n### launchd 設定\n\n```xml\n<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\"\n  \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\">\n<plist version=\"1.0\">\n<dict>\n    <key>Label</key>\n    <string>com.deex.activity-report</string>\n    <key>ProgramArguments</key>\n    <array>\n        <string>/bin/bash</string>\n        <string>-c</string>\n        <string>export PATH=\"/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:$PATH\" &amp;&amp; cd ~/.company &amp;&amp; ./operations/scripts/activity-report.sh</string>\n    </array>\n    <key>StartInterval</key>\n    <integer>21600</integer>\n    <key>StandardOutPath</key>\n    <string>/tmp/deex-activity-report.log</string>\n    <key>StandardErrorPath</key>\n    <string>/tmp/deex-activity-report.err</string>\n    <key>EnvironmentVariables</key>\n    <dict>\n        <key>HOME</key>\n        <string>/Users/knt</string>\n        <key>PATH</key>\n        <string>/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin</string>\n    </dict>\n</dict>\n</plist>\n```\n\n```bash\n# launchd に登録\ncp operations/launchd/com.deex.activity-report.plist ~/Library/LaunchAgents/\nlaunchctl load ~/Library/LaunchAgents/com.deex.activity-report.plist\n\n# 手動実行（テスト用）\ncd ~/.company && ./operations/scripts/activity-report.sh\n```\n\n### Telegram 報告フォーマット\n\n```\n【deex定期報告 14:00】\n✅ 完了: PRM市場最新動向レポート作成 → research/topics/2026-04-10-industry-watch.md\n📊 発見: PartnerStack が $2.7B GTM分析を公開。CoPASS戦略への示唆あり\n🔜 次の6時間: 競合監視（水曜定期）\n⚠️ 要確認: note第4弾の公開承認待ち（#queue）\n```\n\n### タスクプロンプトの書き方\n\n定期タスク（調査・記事作成等）は `operations/task-prompts/` にプロンプトを置く:\n\n```markdown\n# task-prompts/industry-research.md の構造例"
  },
  {
    "title": "目的",
    "tags": [],
    "content": "PRM・パートナービジネス市場のトレンド把握"
  },
  {
    "title": "調査範囲",
    "tags": [],
    "content": "- PRM SaaS製品の動向（新規参入・資金調達・機能追加）\n- SaaS/BPaaS市場全体のトレンド\n- VC・IPO動向（隔週）\n- 海外PRM動向（月次）"
  },
  {
    "title": "出力",
    "tags": [],
    "content": "research/topics/YYYY-MM-DD-industry-watch.md"
  },
  {
    "title": "影響度分類",
    "tags": [],
    "content": "- 🔴 高: CoPASS戦略に直接影響 → Telegram即時通知\n- 🟡 中: 参考情報として記録\n- 🟢 低: 概要のみ"
  },
  {
    "title": "後処理",
    "tags": [],
    "content": "- dashboard.md の業界動向セクションを更新\n- 記事候補を operations/queue/research-tasks.json に追加\n```\n\n### 禁止事項（安全ルール）\n\n- `#human` タグのTODOは絶対に自動実行しない\n- 外部への情報発信を行わない（Slack返信、メール送信、記事公開）\n- オーナー承認待ちの案件を勝手に進めない\n- 大規模な変更を行わない（軽量タスクのみ）\n- `dashboard.md` を更新せずにタスク完了報告しない\n\n### 実行ログの確認\n\n```bash\n# 直近のログ\ntail -50 ~/.company/operations/logs/activity.log\n\n# launchd のエラーログ\ncat /tmp/deex-activity-report.err\n\n# dashboard.md の実行ログセクション\ngrep -A 5 \"自律タスク実行ログ\" ~/.company/operations/dashboard.md\n```"
  },
  {
    "title": "朝まとめ通知と TODO 日次見直しで1日のタスクを把握する",
    "tags": [
      "自律運営",
      "朝まとめ",
      "telegram",
      "todo-review"
    ],
    "content": "毎朝08:00に「今日人間がやるべきこと」をTelegramで通知し、毎晩22:00にTODOの棚卸しを自動実行する。Google Calendar と合わせて使うと、朝の「今日何やるんだっけ」が一瞬で分かる。\n\n### 朝まとめ通知（毎朝 08:00）\n\n`morning-digest.sh` が `#human` の未完了TODOだけを集めて期限順に整理:\n\n1. `secretary/todos/` の全ファイルをスキャン\n2. 未完了 `- [ ]` で `#human`（またはタグなしで人間アクション系）を抽出\n3. `dashboard.md` のブロッカーも確認\n4. 期限別に分類して Telegram 送信\n\n### 通知フォーマット\n\n```\n【朝まとめ 2026-04-10】\n\n🔴 期限超過:\n  ・note第3弾の公開設定（4/8）\n  ・GoCoo CS版テスト結果のフィードバック（4/7）\n\n🟡 今日期限:\n  ・牛丸さんにnote第4弾レビュー結果確認\n\n🔵 今週中:\n  ・SmartCamp配信会議の準備（4/12）\n  ・資本政策スライドのレビュー（4/14）\n\n📋 確認・承認待ち:\n  ・note第4弾 公開キュー（牛丸さん承認待ち）\n\n💡 今日のおすすめアクション:\n  ・牛丸さんへのレビュー確認 → note公開まで一気に進められる\n```\n\n- `#auto` タスクは含めない（そちらは自動実行される）\n- 完了済み `[x]` は含めない\n- 重複は1つにまとめる\n\n### Google Calendar との組み合わせ\n\nClaude Code に「今日のタスク確認して」と言うと:\n\n1. Google Calendar MCP で今日の予定を取得\n2. `secretary/todos/` から今日期限のTODOを取得\n3. 予定とタスクを時系列で並べて表示\n\n```\n今日のスケジュール:\n  10:00 SmartCamp打ち合わせ（Google Meet）\n  14:00 社内定例\n  \n今日のTODO:\n  🔴 note第4弾 公開承認 → 牛丸さん確認後\n  🟡 hacomono再稟議資料の最終確認\n  🔵 業界調査レポートのレビュー（自動実行済み、確認のみ）\n```\n\n### TODO 日次見直し（毎晩 22:00）\n\n`todo-daily-review.sh` が1日の終わりにTODOを棚卸し:\n\n#### 重複検出＆統合\n\n同じ内容のTODOが複数日にまたがって存在する場合、最新日付のものを正として古い方を統合する。\n\n```markdown\n# 統合前（2026-04-08.md と 2026-04-10.md に同じTODOがある）\n- [ ] PRM市場の最新動向を調査 | #auto\n\n# 統合後（2026-04-08.md 側）\n- [x] (統合→2026-04-10) PRM市場の最新動向を調査 | 完了: 2026-04-10\n```\n\nSlackのpermalinkは統合時にも必ず保持する（`ref:` 行やインデント行をセットで移動）。\n\n#### プロジェクト昇格候補の検出\n\n繰り返し登場するテーマをスコアリングで検出:\n\n```\nscore = repeat(0.30) + span(0.25) + multistep(0.20) + team(0.15) + stakeholder(0.10)\n```\n\n- repeat: 同テーマが2日以上出現 → 1.0\n- span: 7日以上の期間にまたがる → 1.0\n- multistep: 複数ステップの依存関係あり → 1.0\n- team: 2人以上の名前が登場 → 1.0\n- stakeholder: 牛丸/東先生等の承認者関与 → 1.0\n\nscore ≥ 0.6 で「TODOではなくプロジェクトとして管理した方がいい」と提案。既に `pm/projects/` にあるテーマは除外。\n\n#### 期限超過チェック\n\n3日以上超過しているTODOに対して:\n- 「完了にすべき」「まだやるべき」「削除すべき」の判断を提案\n- Telegram で報告\n\n### Telegram通知フォーマット（日次見直し）\n\n```\n【TODO日次見直し 2026-04-10】\n\n🔄 重複統合: 2件\n  ・「PRM調査」4/8と4/10 → 4/10に統合\n  ・「GoCoo設定確認」4/6と4/9 → 4/9に統合\n\n📈 プロジェクト昇格候補: 1件\n  ・SmartCamp連携 (score: 0.75)\n    → 4回出現、牛丸さん関与、複数ステップあり\n\n⏰ 期限超過（3日+）: 1件\n  ・note第3弾の公開設定（4/5期限、5日超過）→ 完了にすべき？\n\n📊 全体: 未完了 12件 / うち #auto 4件 / #human 7件 / #queue 1件\n```\n\n### Telegram Bot のセットアップ\n\n```bash\n# 1. @BotFather で Bot を作成し、トークンを取得\n\n# 2. 設定ファイルを作成\ncat > ~/.company/operations/.telegram-config << 'EOF'\nDEEX_TELEGRAM_CHAT_ID=your_chat_id\nDEEX_TELEGRAM_BOT_TOKEN=your_bot_token\nEOF\n\n# 3. テスト送信\n./operations/scripts/notify-telegram.sh \"テスト通知\"\n```\n\n- チャットIDは Bot にメッセージを送った後、`https://api.telegram.org/botTOKEN/getUpdates` で確認\n- 4000文字制限あり（自動で切り詰め）\n\n### launchd 設定\n\n```bash\n# 朝まとめ: 毎朝 08:00\nlaunchctl load ~/Library/LaunchAgents/com.deex.morning-digest.plist\n\n# 日次見直し: 毎晩 22:00\nlaunchctl load ~/Library/LaunchAgents/com.deex.todo-daily-review.plist\n```\n\n### 実績\n\n- 2026年3月〜 稼働中（Mac Mini で常時実行）\n- 朝まとめで毎朝 5〜15件の #human TODO を通知\n- 日次見直しで週に 3〜5件の重複統合を自動実行\n- プロジェクト昇格提案 → 実際に pm/projects/ に移行した実績あり（SmartCamp連携、資本政策更新等）"
  }
]